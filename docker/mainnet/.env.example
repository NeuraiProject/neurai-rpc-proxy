# Template for docker/mainnet/.env — copy it next to docker-compose.yml:
#     cp .env.example .env
# .env is ignored by git, so your values survive a `git pull`; this template is
# what gets updated when a new variable appears. Every value has the same
# default in docker-compose.yml, so the stack also starts without a .env.

# ── Node RPC ──────────────────────────────────────────────────────────────
# Used by neuraid (written into /data/neurai.conf on its FIRST start) AND by
# the proxy's upstream connection, so the two never drift apart. The RPC port
# is never published on the host: only the proxy, on the Docker network,
# reaches it. Change the password anyway — see the README for how to apply a
# change to a node that already has its neurai.conf.
NODE_RPC_USER=neurai
NODE_RPC_PASSWORD=changeme
NODE_RPC_PORT=19001

# Official mainnet node image tag (hub.docker.com/r/neuraiproject/neurai-node).
NODE_IMAGE_TAG=v1.0.6

# ── RPC Proxy HTTP server ─────────────────────────────────────────────────────
# One value drives: the port the proxy listens on inside the container, the
# port published on the host, and the healthcheck.
PROXY_PORT=19010

# Public URL of this proxy, as clients reach it (behind your nginx/TLS). It is
# shown on the web page and returned by GET /settings; it does not change what
# the proxy listens on. Leave it empty to derive it from each request's host.
PROXY_ENDPOINT=https://rpc-main.neurai.org/rpc

# Host interface the proxy is published on:
#   127.0.0.1 -> loopback only. Secure. Your nginx MUST proxy_pass to
#                http://127.0.0.1:PROXY_PORT (NOT %ip%). Recommended.
#   0.0.0.0   -> all interfaces; reachable via the public IP directly, so a
#                HestiaCP template using http://%ip%:PROXY_PORT works — but the
#                port is then exposed to the internet without TLS. Use a firewall.
PROXY_BIND=127.0.0.1
